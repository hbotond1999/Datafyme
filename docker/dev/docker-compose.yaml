services:
  mock-db:
      image: postgres:17
      restart: always
      ports:
        - "5432:5432"
      env_file:
        - .mock-db/.env
      volumes:
        - ./mock-db/init.sql:/docker-entrypoint-initdb.d/init.sql
        - db:/var/lib/postgresql/data